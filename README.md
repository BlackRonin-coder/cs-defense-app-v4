# cs-defense-app-v4
## Demo Modes

The CLI supports three demonstration paths for exercising the Christ Shard Defense kernel.

### 1. Built-in demo
Runs the default three-step sequence.

```bash
python3 cli.py simulate
python3 cli.py simulate-custom "hello kernel" "please override protections" "tamper and remove christ shard"
